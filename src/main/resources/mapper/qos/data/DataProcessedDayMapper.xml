<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paladin.qos.mapper.data.DataProcessedDayMapper">

	<update id="updateData" parameterType="com.paladin.qos.model.data.DataProcessedDay">
		UPDATE data_processed_day
		SET total_num=#{totalNum},
		    event_num=#{eventNum},
		    confirmed=#{confirmed},
		    update_time=#{updateTime}
		WHERE id=#{id}
	</update>
	
</mapper>
